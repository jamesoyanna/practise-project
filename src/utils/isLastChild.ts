// Function to check if the provided index is the last index in the array
export default function isLastChild(arr: Array<unknown>, index: number) {
    // Check if the provided index is the last index in the array
    return arr.length === index + 1
}
